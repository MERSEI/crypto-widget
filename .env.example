# crypto-widget has no secrets in the MVP: every Binance and FX endpoint it calls is public.
# These are optional overrides, only useful for testing against a different host/mirror.

# BINANCE_REST_BASE_URL=https://api.binance.com
# BINANCE_WS_BASE_URL=wss://stream.binance.com:9443/stream
# EXCHANGERATE_HOST_URL=https://api.exchangerate.host
